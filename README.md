# TomorrowUGameRepo

Current Status:
Project files have been recovered but due to method they are in disarray and need to be fixed before the github pages will fully work, however the pages is setup so that when the game works the pages will be ready.

Porting to the Web:
The repo is using unity pages to port it the the web which is configured to run through the output file and the index.html file as long as you keep updating the project file the pages will be updated.

Editing:
1. To make edit to the game you will need to download ExportedProject and then add that into github to edit it. 
2. I use github desktop to make it easier to upload edits to the github repo and sometimes have to use git bash for files that are too big

Future Notes:
1. The assets and the code are in the ExportedProject, however the scenes are not fully connected even though there is no errors.
2. This is due to the method we used to port the original game from the .exe file to a working unity file.
3. For any future members working on this project I would suggest trying to work with the scenes so that the game runs smoothly.
4. Once the scenes are running then you just need to edit the clients website so that it runs a page based on the github pages url.
